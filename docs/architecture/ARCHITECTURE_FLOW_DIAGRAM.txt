```
╔══════════════════════════════════════════════════════════════════════════════════════════════════════════════════╗
║                  AI LOAD BALANCER - COMPLETE ARCHITECTURE & DATA FLOW                                          ║
║                         From PowerShell Setup to Real-Time Dashboard                                           ║
╚══════════════════════════════════════════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                                    PHASE 1: POWERSHELL SETUP                                                   │
└─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

        ┌──────────────────────┐
        │   Developer/User     │
        │  (PowerShell Admin)  │
        └──────────┬───────────┘
                   │
                   ├─► .\setup-tools.ps1
                   │   └─► Downloads & extracts all 5 services to ./bin
                   │       ├─ redis-server.exe
                   │       ├─ prometheus.exe
                   │       ├─ loki-windows-amd64.exe
                   │       ├─ promtail-windows-amd64.exe
                   │       └─ nginx.exe
                   │
                   ├─► .\start-all-services.ps1
                   │   └─► Orchestrates startup of all 7 services
                   │
                   └─► .\start-services-monitor.ps1
                       └─► Displays real-time service status


┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                              PHASE 2: SERVICES LAYER (Port Mapping)                                            │
└─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

    LOCAL MACHINE: 127.0.0.1 / localhost

    ┌──────────────────────────────────────────────────────────────────────────────────────────────────┐
    │ EXTERNAL SERVICES (Background / Hidden)                                                         │
    ├──────────────────────────────────────────────────────────────────────────────────────────────────┤
    │                                                                                                  │
    │  Redis            Prometheus      Loki            Promtail           NGINX                      │
    │  Port 6379        Port 9090       Port 3100       Port 9080          Port 80                    │
    │  ┌─────────┐     ┌─────────┐    ┌─────────┐     ┌─────────┐       ┌─────────┐                │
    │  │ Cache & │     │ Metrics │    │ Logs    │     │ Log     │       │ Reverse │                │
    │  │ Session │     │ Storage │    │ Storage │     │ Shipper │       │ Proxy   │                │
    │  │ Store   │     │ & Query │    │ & Query │     │         │       │         │                │
    │  └─────────┘     └─────────┘    └─────────┘     └─────────┘       └─────────┘                │
    │                                                                                                  │
    └──────────────────────────────────────────────────────────────────────────────────────────────────┘
                      ▲                ▲                ▲                    ▲
                      │ Stores         │ Scrapes        │ Queries            │ Proxies
                      │ Metrics        │ Metrics        │ Logs               │ Requests
                      │                │                │                    │
    
    ┌──────────────────────────────────────────────────────────────────────────────────────────────────┐
    │ APPLICATION LAYER (Visible)                                                                     │
    ├──────────────────────────────────────────────────────────────────────────────────────────────────┤
    │                                                                                                  │
    │  Backend API (Express.js)         Dashboard (React/Vite)                                       │
    │  Port 8000                         Port 3000                                                    │
    │  ┌─────────────────────────────┐  ┌─────────────────────────────┐                             │
    │  │  Core Endpoints:            │  │  Components:                │                             │
    │  │ • /metrics                  │  │ • ServicesMonitor           │                             │
    │  │ • /api/v1/services/status   │  │ • MetricsPanel              │                             │
    │  │ • /api/v1/metrics           │  │ • LogsViewer                │                             │
    │  │ • /api/v1/logs              │  │ • PerformanceCharts         │                             │
    │  │ • ... (other routes)        │  │ • AlertsPanel               │                             │
    │  └─────────────────────────────┘  └─────────────────────────────┘                             │
    │                                                                                                  │
    └──────────────────────────────────────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                           PHASE 3: DATA FLOW & REAL-TIME UPDATES                                              │
└─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

DATA FLOW 1: SERVICE HEALTH STATUS (Every 3 seconds)
═════════════════════════════════════════════════════

    Dashboard (3000)
       │
       ├─ Auto-refresh timer (3s)
       │
       ├─ fetch('/api/v1/services/status')
       │
       ↓
    Backend (8000)
       │
       ├─ Check: Redis (6379) → TCP connection
       ├─ Check: Prometheus (9090) → HTTP /api/v1/status
       ├─ Check: Loki (3100) → HTTP /loki/api/v1/status
       ├─ Check: Promtail (9080) → TCP/HTTP check
       ├─ Check: NGINX (80) → HTTP request
       └─ Check: Dashboard (3000) → HTTP request
       │
       ↓
    Response JSON:
    {
      "summary": { "total": 7, "running": 6, "stopped": 1, "timeout": 0 },
      "services": [
        {
          "id": "redis",
          "name": "Redis",
          "host": "localhost",
          "port": 6379,
          "status": "running",
          "responseTime": 2,
          "uptime": "2h 15m"
        },
        ... (6 more services)
      ]
    }
       │
       ↓
    ServicesMonitor Component (React)
       │
       └─ Updates status cards with real-time data
          Displays: ✓ Running / ✗ Offline / ⚠ Timeout


DATA FLOW 2: METRICS & PERFORMANCE (Every 5 seconds)
══════════════════════════════════════════════════════

    Backend Application (Running)
       │
       ├─ Tracks: API requests, response times, errors, cache hits
       ├─ Tracks: Database queries, load balancer decisions, AI model latency
       └─ Exposes: /metrics endpoint (Prometheus format)
       │
       ↓
    Prometheus (9090)
       │
       ├─ Scrapes /metrics every 15 seconds
       ├─ Stores time-series data
       └─ Provides query API (/api/v1/query)
       │
       ↓
    Dashboard (3000)
       │
       ├─ fetch('/api/v1/metrics')
       │
       ↓
    Backend (8000)
       │
       ├─ Queries Prometheus (/api/v1/query)
       ├─ Aggregates data (p50, p95, p99 latencies)
       └─ Returns formatted JSON for charts
       │
       ↓
    MetricsPanel Component (React)
       │
       └─ Renders real-time charts:
          ├─ API Response Time (line chart)
          ├─ Request Rate (bar chart)
          ├─ Error Rate (line chart)
          ├─ Cache Hit Ratio (gauge)
          └─ Active Connections (line chart)


DATA FLOW 3: LOG AGGREGATION (Every 10 seconds)
═════════════════════════════════════════════════

    Backend Application (Running)
       │
       ├─ Logs to: ./logs/natour-log.json
       │
       ↓
    Promtail (Log Shipper)
       │
       ├─ Tails log files
       ├─ Parses JSON logs
       └─ Ships to Loki (3100)
       │
       ↓
    Loki (3100)
       │
       ├─ Stores & indexes logs
       ├─ Makes searchable
       └─ Provides query API (/loki/api/v1/query_range)
       │
       ↓
    Dashboard (3000)
       │
       ├─ fetch('/api/v1/logs?since=5m&limit=100')
       │
       ↓
    Backend (8000)
       │
       ├─ Queries Loki with LogQL
       ├─ Filters & sorts logs
       └─ Returns formatted JSON
       │
       ↓
    LogsViewer Component (React)
       │
       └─ Displays real-time logs:
          ├─ Timestamp
          ├─ Log level (INFO, WARN, ERROR)
          ├─ Service name
          └─ Log message


┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                         PHASE 4: BROWSER RENDERING & INTERACTIONS                                             │
└─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

USER OPENS: http://localhost:3000 (Browser)
════════════════════════════════════════════

    ┌─────────────────────────────────────────────────────────────────────────┐
    │                        DASHBOARD (React UI)                             │
    ├─────────────────────────────────────────────────────────────────────────┤
    │                                                                         │
    │  ┌─────────────────────────────────────────────────────────────────┐  │
    │  │  Header: "AI Load Balancer Dashboard"                          │  │
    │  │  Status: ✓ 6/7 Services Running | Last Updated: 2 seconds ago │  │
    │  └─────────────────────────────────────────────────────────────────┘  │
    │                                                                         │
    │  ┌──────────────────────┐ ┌──────────────────────┐ ┌──────────────┐  │
    │  │  ServicesMonitor Tab │ │  Metrics Tab         │ │  Logs Tab    │  │
    │  └──────────────────────┘ └──────────────────────┘ └──────────────┘  │
    │                                                                         │
    │  ┌────────────────────────────────────────────────────────────────┐   │
    │  │               SELECTED TAB: ServicesMonitor                    │   │
    │  ├────────────────────────────────────────────────────────────────┤   │
    │  │                                                                │   │
    │  │  SERVICES GRID (7 Cards, Auto-refresh every 3s)             │   │
    │  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐         │   │
    │  │  │   Redis      │ │ Prometheus   │ │    Loki      │         │   │
    │  │  │ ✓ Running    │ │ ✓ Running    │ │ ✓ Running    │         │   │
    │  │  │ 6379         │ │ 9090         │ │ 3100         │         │   │
    │  │  │ 2h 15m       │ │ 2h 15m       │ │ 2h 15m       │         │   │
    │  │  └──────────────┘ └──────────────┘ └──────────────┘         │   │
    │  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐         │   │
    │  │  │  Promtail    │ │    NGINX     │ │   Backend    │         │   │
    │  │  │ ✓ Running    │ │ ✓ Running    │ │ ✓ Running    │         │   │
    │  │  │ 9080         │ │ 80           │ │ 8000         │         │   │
    │  │  │ 2h 15m       │ │ 2h 15m       │ │ 2h 15m       │         │   │
    │  │  └──────────────┘ └──────────────┘ └──────────────┘         │   │
    │  │  ┌──────────────┐                                            │   │
    │  │  │  Dashboard   │                                            │   │
    │  │  │ ✓ Running    │                                            │   │
    │  │  │ 3000         │                                            │   │
    │  │  │ 2h 15m       │                                            │   │
    │  │  └──────────────┘                                            │   │
    │  │                                                                │   │
    │  │  Quick Actions: [Start] [Stop] [Restart] [Refresh]          │   │
    │  │                                                                │   │
    │  └────────────────────────────────────────────────────────────────┘   │
    │                                                                         │
    │  ┌────────────────────────────────────────────────────────────────┐   │
    │  │  SUMMARY SECTION                                               │   │
    │  │  ✓ Running: 6/7 (86%)  | ⏱ Avg Response: 45ms | 🟢 Healthy  │   │
    │  └────────────────────────────────────────────────────────────────┘   │
    │                                                                         │
    └─────────────────────────────────────────────────────────────────────────┘

    USER CLICKS "Metrics Tab":
    ═══════════════════════════

    ┌────────────────────────────────────────────────────────────────┐
    │               SELECTED TAB: Metrics (Charts)                   │
    ├────────────────────────────────────────────────────────────────┤
    │                                                                │
    │  API Response Time (ms)        Request Rate (req/sec)         │
    │  ┌──────────────────────┐     ┌──────────────────────┐       │
    │  │                      │     │ 250 ─┐              │       │
    │  │ 100 ─┐               │     │      │              │       │
    │  │      │          ╱\   │     │      └──┐           │       │
    │  │   50 ┼────────╱  \──╱\───▼│  100    │╱\         │       │
    │  │      │              │     │        │  \╱\       │       │
    │  │    0 └──────────────▼─────│        └─────▼──────▼│       │
    │  │      0    5    10   15    │           0  5  10  15      │
    │  └──────────────────────────┘     └──────────────────────────┘
    │                                                                │
    │  Cache Hit Ratio (%)           Error Rate (%)                │
    │  ┌──────────────────────┐     ┌──────────────────────┐       │
    │  │ ████████████░░ 92%  │     │ ▁▁ 0.2% ▁▁          │       │
    │  └──────────────────────┘     └──────────────────────┘       │
    │                                                                │
    │  Refresh Interval: [Every 5 seconds]  Last Updated: 2s ago  │
    │                                                                │
    └────────────────────────────────────────────────────────────────┘

    USER CLICKS "Logs Tab":
    ══════════════════════

    ┌────────────────────────────────────────────────────────────────┐
    │               SELECTED TAB: Logs (Real-Time Stream)            │
    ├────────────────────────────────────────────────────────────────┤
    │                                                                │
    │  Filters: [All] [INFO] [WARN] [ERROR]  Search: [________]   │
    │                                                                │
    │  TIME              LEVEL    MESSAGE                           │
    │  ─────────────────────────────────────────────────────────── │
    │  2025-01-21        ✓ INFO    Server started successfully      │
    │  14:32:15          ✓ INFO    Connected to Redis cache        │
    │  14:32:20          ✓ INFO    Prometheus metrics enabled      │
    │  14:32:25          ⚠ WARN    Slow query detected (250ms)    │
    │  14:32:30          ✓ INFO    Request processed: /api/tours   │
    │  14:32:35          ✓ INFO    Response time: 45ms             │
    │  14:32:40          ✓ INFO    Cache hit: user:123:tours       │
    │  14:32:45          ✗ ERROR   Database connection timeout     │
    │  14:32:50          ✓ INFO    Reconnected to database         │
    │                                                                │
    │  [Load More] Auto-refresh: ✓ Enabled  Refresh Rate: 10s     │
    │                                                                │
    └────────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                              COMPLETE DATA FLOW SUMMARY                                                        │
└─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

START-UP SEQUENCE:
═════════════════
User → .\start-all-services.ps1 → Download services → Start 7 services → Open http://localhost:3000

REAL-TIME LOOP (Continuous):
═════════════════════════════
Dashboard (React)
    ↓ (Every 3s)
    └─→ /api/v1/services/status → ServicesMonitor updates
    
Dashboard (React)
    ↓ (Every 5s)
    └─→ /api/v1/metrics → MetricsPanel updates charts
    
Dashboard (React)
    ↓ (Every 10s)
    └─→ /api/v1/logs → LogsViewer updates log stream

Backend (Express)
    ↓ (Continuous)
    ├─ Runs application logic
    ├─ Generates metrics → /metrics endpoint
    ├─ Checks services → /api/v1/services/status
    ├─ Queries Prometheus → /api/v1/metrics
    └─ Queries Loki → /api/v1/logs

External Services (Continuous)
    ├─ Redis: Stores sessions & cache
    ├─ Prometheus: Collects & stores metrics
    ├─ Loki: Stores & indexes logs
    ├─ Promtail: Ships logs to Loki
    └─ NGINX: Proxies requests


NETWORK TOPOLOGY:
════════════════
┌──────────────┐
│   Browser    │ http://localhost:3000
│  (localhost) │
└──────┬───────┘
       │
       ├─ Fetch /api/v1/services/status      (3s interval)
       ├─ Fetch /api/v1/metrics              (5s interval)
       └─ Fetch /api/v1/logs                 (10s interval)
       │
       ↓
┌──────────────────────────────────────────────────────────────┐
│  Backend API (Port 8000)                                     │
├──────────────────────────────────────────────────────────────┤
│  ├─ Checks: Redis, Prometheus, Loki, Promtail, NGINX, self  │
│  ├─ Queries: Prometheus API (port 9090)                      │
│  ├─ Queries: Loki API (port 3100)                            │
│  └─ Exposes: /metrics (Prometheus format)                    │
└──────┬────┬─────────┬──────────┬──────────────────────┘
       │    │         │          │
       ↓    ↓         ↓          ↓
    Redis  Prom      Loki     Promtail
    6379   9090      3100      9080


KEY FEATURES:
═════════════
✓ Automatic service startup via PowerShell
✓ Real-time service health monitoring (3s refresh)
✓ Live performance metrics & charts (5s refresh)
✓ Log streaming & search capability (10s refresh)
✓ Multi-service orchestration
✓ Zero downtime monitoring
✓ Graceful error handling
✓ Beautiful React UI with real-time updates


SUCCESS INDICATORS:
═══════════════════
1. All 7 services show ✓ Running in dashboard
2. Charts updating with live data
3. Logs appearing in real-time stream
4. No errors in browser console
5. Response times < 1 second
6. Smooth UI updates without lag
```
